import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Legna AI – Ethiopia’s Multilingual Business Assistant",
  description: "Legna AI is a multilingual business assistant built for Ethiopian entrepreneurs. It helps users generate marketing content, translate business messages, communicate with customers, and access business strategies—in Amharic, Tigrinya, Afan Oromo, Somali, and English. Using AI and Google Cloud, it breaks the language barrier and empowers businesses to scale.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
